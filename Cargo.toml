[package]
name = "merlion-kernel"
version = "0.1.0"
edition = "2021"
authors = ["MerlionOS Contributors"]
description = "A Singapore-inspired hobby operating system kernel"

[dependencies]
bootloader = "0.9"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
